{
  "name": "ghostinit",
  "version": "0.1.0",
  "private": false,
  "description": "Production-grade Bun-powered CLI that generates well-structured monorepos with architectural linting (inspired by modular monolith, build-time enforcement via oxc-parser) for Next.js/TanStack Start.",
  "license": "MIT",
  "bin": {
    "ghostinit": "dist/cli.js"
  },
  "workspaces": [
    "packages/*",
    "tooling/*"
  ],
  "files": [
    "dist/cli.js",
    "src/**/*",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "type": "module",
  "main": "dist/cli.js",
  "exports": {
    ".": {
      "default": "./dist/cli.js"
    }
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun run --watch src/cli.ts",
    "typecheck": "tsc -b",
    "check": "oxlint . && oxfmt --check . && tsc --noEmit",
    "format": "oxfmt --write .",
    "pretest": "bun run build",
    "test": "bun test --timeout 100000 tests/integration tests/unit",
    "pretest:fixtures": "cd tests/fixtures/compatibility/drizzle-betterauth-orpc && bun install && cd ../next-tailwind-biome && bun install",
    "test:fixtures": "bun test tests/fixtures",
    "test:ci": "bun run test && bun run pretest:fixtures && bun run test:fixtures && bun run test:generated -- --all",
    "test:generated": "bun run scripts/test-generated.ts",
    "check:versions": "bun run scripts/check-versions.ts",
    "release": "bun run check:versions && bun run build && bun run test:generated -- --workers && bun pm pack"
  },
  "dependencies": {
    "@clack/prompts": "0.8.2",
    "oxc-parser": "0.139.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@chargily/chargily-pay": "2.1.0",
    "@orpc/contract": "^1.14.7",
    "@orpc/server": "^1.14.7",
    "@orpc/zod": "^1.14.7",
    "bun-types": "1.4.0",
    "drizzle-orm": "^0.45.2",
    "oxfmt": "0.58.0",
    "oxlint": "1.73.0",
    "typescript": "6.0.3"
  },
  "engines": {
    "bun": ">=1.4.0",
    "node": ">=22.0.0"
  },
  "packageManager": "bun@1.4.0",
  "_comment_private": "Root IS the publishable CLI (bin ghostinit). Keep private false so npm publish works. Bun allows workspaces with private false - this is intentional, not a monorepo root that is never published."
}
